import React from 'react'
import ImageCarousel from './ImageCarousel'

export const Layout = () => {
    return (
        <div>
            <ImageCarousel/>
        </div>
    )
}

export default Layout;
